`just --
`nothing -- `nothing

join : Maybe(Maybe(a)) -- Maybe(a)
     := [ { `just⁻¹ & `nothing⁻¹ `nothing } ]

'A {a b `t ⊕ a a `f} and a a B
(split)


`e --
{`e ⊕ `a ⊕ `b ⊕ `c} `a -- {`e ⊕ `a ⊕ `b ⊕ `c}

1 0
2 1

a `just -- Bool
`nothing -- Bool

2 1
1 1

`just --
`nothing -- `nothing
1 0
1 1

sol'n: A `just -- A
       B `nothing -- `nothing B

instead of A=B

`just --
`nothing -- `nothing

join : Maybe(Maybe(a)) -- Maybe(a)
     := [ { `just⁻¹ & `nothing⁻¹ `nothing } ]

`just --
`nothing -- `nothing

sol'n: A `just -- A
       C `nothing -- C `nothing

instead of A=C, then in matching w/ Maybe(a) aka C=ø, A=Maybe(a)

write B ⊂ { `just -- ø, `nothing -- `nothing}

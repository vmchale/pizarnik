@i prelude/fn
@i prelude/bool
@i lib/either
@i lib/both

x : -- Either(Bool,Int)
  := [ True `left ]

b : -- Both(Bool,Int)
  := [ [ not ] x mapL ]

c : Both(Bool, Int) -- Either(Bool,Int)
  := [ [ not ] swap mapLeft ]

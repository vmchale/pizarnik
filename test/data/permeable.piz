@i prelude/fn
@i lib/list

type Unit = {`unit};

x : -- NE(Unit)
  := [ `nil `unit `cons ]

# is { x & y } like par-quote?

y : -- Unit
  := [ x head ]

z : -- Unit
  := [ [nip] `unit x foldr ]

x_ : -- List(Unit)
   := [ `nil ]

w : -- Unit
  := [ x_ head ]

@i prelude/fn
@i lib/these

%-

# TODO: make work with Either? could be fun...
mapThisThat : [a -- b] { a `this ⊕ c `that } -- { b `this ⊕ c `that }
            := [ { `this⁻¹ swap $ `this
                 & `that⁻¹ nip `that }
               ]

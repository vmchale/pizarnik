@i prelude/fn
@i lib/either

%-

# TODO: make work with Either? could be fun...
mapLeftRight : [a -- b] { a `left ⊕ c `right } -- { b `left ⊕ c `right }
             := [ { `left⁻¹ swap $ `left
                  & `right⁻¹ nip `right }
                ]

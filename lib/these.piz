@i prelude/fn
@i lib/either

%-

type Both a b = Either(a,b) ∪ { a b `both };

map1 : [a -- b] Both(a,c) -- Both(b,c)
     := [ { mapLeft
          & `both⁻¹ [swap $] dip `both }
        ]

bimap : [a -- b] [c -- d] Both(a,b) -- Both(c,d)
      := [ { `left⁻¹ nip swap $ `left
           & `right⁻¹ nip2 swap $ `right
           & `both⁻¹ [rotl] dip [$] dip2 rot [swap $] dip swap `both
           } ]

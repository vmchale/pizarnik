@i prelude/fn

%-

# FIXME: % is not remainder but "interger divide" (!)
gcd : Int Int -- Int
    := [ dup 0 = { True⁻¹ drop & False⁻¹ dup [%] dip swap gcd } ]

divides : Int Int -- Bool
        := [ % 0 = ]
